# Five Skeptical Reviewer Reports (semantic-identity/HOSTILE_REVIEWS.md)

## 1. Reviewer 1 — Programming Languages
- **Argument**: *"Semantic Identity is simply dynamic refinement type checking serialized alongside data bytes. Rice's theorem makes general program semantic hashing undecidable."*
- **Verdict**: **REJECT AS NEW PL PRIMITIVE**. Classify as serialized dynamic contract abstraction.

## 2. Reviewer 2 — Formal Methods
- **Argument**: *"Syntactic equality ($H_{\text{semantic}}$) does not equal semantic equivalence. Two expressions (`x >= 0` and `NOT(x < 0)`) have distinct semantic hashes despite identical logical semantics."*
- **Verdict**: **REJECT AS SYNTACTIC HASHING**. Classify as canonical AST contract identity.

## 3. Reviewer 3 — Distributed Systems
- **Argument**: *"Nix derivations and Bazel action keys already hash build commands alongside input artifacts. This is established build graph engineering."*
- **Verdict**: **REJECT AS UNPRECEDENTED**. Classify as content-addressed contract hash.

## 4. Reviewer 4 — Database Systems
- **Argument**: *"SQLite CHECK constraints and Postgres assertions enforce invariants over tuples. Extracting them to JSON is an open protocol format."*
- **Verdict**: **REJECT AS NEW DB PRIMITIVE**. Classify as portable file format.

## 5. Reviewer 5 — Cryptography & Security
- **Argument**: *"Coupling code and data into a single hash payload is the exact architecture of Ethereum smart contract codeHash + stateRoot."*
- **Verdict**: **REJECT AS NEW CRYPTO PRIMITIVE**. Classify as offline smart contract envelope.

---

## 🔒 Master Hostile Reviewer Verdict
**Semantic Identity is NOT a new hardware, kernel, or PL instruction set primitive (Level A)**.  
It is a **NOVEL COMPUTATIONAL ABSTRACTION (Level B)** and **OPEN PROTOCOL FORMAT (Level C)**.
